For the PDF downloader install 'npm install jspdf --save' and 'npm install @types/jspdf --save-dev'.
Fot the Star rating install npm install ng-starrating.
For the ngx-toastr(success messages) install npm install ngx-toastr --save and npm install @angular/animations --save.